<%= render 'admin/shared/configuration_menu' %>

<div class='toolbar'>
  <ul class='actions'>
    <li>
      <%= button_link_to t("new_slide"), new_object_url, :icon => 'add'  %>
    </li>
  </ul>
  <br class='clear' />
</div>

<h1><%= t("slides") %></h1>

<table class="index">
  <thead>
    <tr>     
      <th><%= t("name") %></th>
      <th><%= t("groups") %></th>
      <th><%= t("enabled") %></th>
      <th><%= t("included") %></th>
      <th><%= t("position") %></th>     
      <th><%= t("action") %></th>
    </tr>
  </thead>
  <tbody>
    <% @slides.each do |slide|%>
      <tr class="<%= cycle('even', 'odd') %>" id="<%= dom_id slide %>">     
          <td><%= slide.name %></td>
          <td><%= slide.groups %></td>
          <td><%= slide.enabled ? t(:yes) : t(:no) %></td>
          <td><%= slide.included ? t(:yes) : t(:no) %></td>
	  	  <td><%= slide.position %></td>
        <td>
        	<%= link_to_edit slide %> &nbsp;
        	<%= link_to_delete slide %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
